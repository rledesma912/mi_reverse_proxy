services:
  reverse-proxy:
    image: nginx:latest
    container_name: mi_reverse_proxy
    ports:
      - "80:80"   # Puerto estándar para HTTP
      - "443:443" # Puerto estándar para HTTPS
    volumes:
      # Monta la configuración de Nginx
      - ./nginx_config:/etc/nginx/conf.d
      # Monta los certificados SSL
      - ./nginx_certs:/etc/nginx/certs
    # 'host.docker.internal' requiere esta configuración para ser accesible
    # desde el contenedor de Nginx. 'host-gateway' es la IP del host.
    extra_hosts:
      - "host.docker.internal:host-gateway"
